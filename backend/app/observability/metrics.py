def record_event_published(event_type: str, routing_key: str) -> None:
    # Placeholder for Prometheus counters in the next observability iteration.
    del event_type, routing_key


def record_event_processed(event_type: str, routing_key: str) -> None:
    # Placeholder for worker processing metrics.
    del event_type, routing_key

